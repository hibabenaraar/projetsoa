spring.application.name=gateway-service

server.port=8090

spring.cloud.discovery.enabled=true
eureka.client.enabled=true

# Routes are defined in code: gateway_service/.../GatewayConfig.java
# spring-cloud-gateway-server-webmvc does NOT use spring.cloud.gateway.routes[] from properties
# (those keys are for the reactive gateway starter, which this project does not use).
